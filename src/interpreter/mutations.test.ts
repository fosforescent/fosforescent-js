import { Store } from '../dag-implementation/store'
import { IFosInterpreter } from "../types" 
import { Fos } from '..'

// import { createTaskMutation } from './mutations'

describe('mutations', () => {


  test.skip('can create task', () => {    
    const fos = Fos()

  //   const interpreter = fos.startTransaction()



  //   const children = fos.query()
  //   expect(children.length).toBe(1)

  //   const taskMutation = createTaskMutation('B') 
  //   const newInterpreter = interpreter.mutate(taskMutation)

  //   const newFos = fos.commit(newInterpreter)
   
  //   const newFosChildren = newFos.query()

  //   expect(newFosChildren.length).toBe(2)
  //   expect(newFosChildren[1].getName()).toBe('B')
  })

  


})